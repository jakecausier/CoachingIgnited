# CoachingIgnited Theme

Install onto a server and run through the WordPress setup.

A plugins folder may need to be created before they work, as WordPress might not have permissions to create one itself.